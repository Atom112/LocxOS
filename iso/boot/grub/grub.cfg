# 启动菜单等待时间（秒）。0 表示不等待，直接进入默认项。
set timeout=0
# 默认启动第 0 个菜单项。
set default=0

# 最小启动项：通过 Multiboot2 加载内核 ELF。
menuentry "LocxOS" {
	# /boot/kernel.elf 位于 ISO 镜像内路径。
	multiboot2 /boot/kernel.elf
	boot
}
